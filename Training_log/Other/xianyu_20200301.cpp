/* basic header */
#include <bits/stdc++.h>
/* define */
#define ll long long
#define pb emplace_back
#define mp make_pair
#define eps 1e-8
#define lson (curpos<<1)
#define rson (curpos<<1|1)
/* namespace */
using namespace std;
/* header end */

vector<pair<int, int>>interval;

int main() {
    int n;
    cin >> n;
    interval.resize(n);
    for (auto &i : interval) cin >> i.first >> i.second;
    sort(internal.begin(), internal.end());
    return 0;
}